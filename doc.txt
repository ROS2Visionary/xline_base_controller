source install/setup.sh
ros2 run xline_base_controller base_controller_node

colcon build
source install/setup.sh
ros2 run xline_inkjet_printer inkjet_printer_node

xline_follow_controller xline_msgs  xline_base_controller
colcon build --packages-select xline_follow_controller xline_base_controller

colcon build --parallel-workers 1


ros2 service call /motion_control/execute_calibration std_srvs/srv/Trigger


#  设置工作空间根
export XLINE_WS_ROOT=/root/xline_ws/xline_base_controller

export XLINE_WS_ROOT=/root/xline_ws/_xline_data_dir

  1. 使用指令名称
  source install/setup.sh
  ros2 service call /printer/send_command xline_msgs/srv/PrinterCommand \
    "{printer_name: 'printer_center', json_data: '{\"EU2L\":{\"noises\":1}}'}"

  source install/setup.sh
  ros2 service call /printer/send_command xline_msgs/srv/PrinterCommand \
    "{printer_name: 'printer_center', json_data: '{\"EU2L\":{\"setupEvent\":1}}'}"

  source install/setup.sh
  ros2 service call /printer/send_command xline_msgs/srv/PrinterCommand \
    "{printer_name: 'printer_center', json_data: '{\"EU2L\":{\"setupEvent\":0}}'}"



  5. 测试打印指令
  ros2 service call /printer/quick_command xline_msgs/srv/QuickCommand \
    "{action: 'test', printer_name: 'center', param: 0}"


如果不是特别说明，无需编译，无需编写测试代码，无需编写文档


1、只在tcp连接时不执行ping,其它时候每隔2s执行一次ping
2、增加多一个属性用来标识ip是否在线
3、状态话题删除字段'status_code''status_message'以及与它们相关的代码
4、状态话题新增第2步中新增的属性



ssh xline@192.168.0.123

cd zyq_ws/
bash kill.sh

colcon build
source install/setup.sh
ros2 run xline_path_planner planner_node   >/dev/null 2>&1 &


lookahead = ref_lookahead × (v/ref_v) × (r/ref_r)^α


想“更快回线”：增大 guidance.stanley_k，但同时用 guidance.theta_max 限制上限。
想“更稳不抖”：增大 guidance.stanley_v0、减小 k，或加大 theta_max 的约束、并确保 max_angular_vel/max_angular_accel 合理。
如果你的定位横向噪声较大，weighted_yaw 可能更耐受；如果偏线大、需要快速切回线，stanley 通常更占优。



localization:
  ros__parameters:
    # 反射板到机器人基座的坐标偏移量 (单位: 米)
    # 这些参数定义了反射板(激光靶球)相对于机器人基座(base_link)的位置
    reflector_to_base_x:  0.0895
    reflector_to_base_y: 0.0
    reflector_to_base_z: 0.0

