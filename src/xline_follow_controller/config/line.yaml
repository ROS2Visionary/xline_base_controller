
# 调试配置
debug:
  enabled: true
  raw_path: "xline_follow_controller/path_l"
  filtered_path: "xline_follow_controller/path_sg"


# 航向PID控制
heading_pid:
  alignment:                         # 对齐阶段
    kp: 1.0
    ki: 0.0
    kd: 0.1
  following:                         # 跟随阶段
    kp: 1.0
    ki: 0.0
    kd: 0.1


# 速度控制
velocity:
  # 速度限制
  accel_min: 0.05                    # 加速时最小线速度 [m/s]
  decel_min: 0.03                    # 减速时最小线速度 [m/s]
  walk_max: 0.35                     # 步行模式最大速度 [m/s]
  work_max: 0.35                     # 工作模式最大速度 [m/s]
  alignment: 0.05                    # 对齐阶段速度 [m/s]
  # 调速参数
  acce_factor: 0.002                # 加速因子
  accel_sigmoid_k: 6.0               # 加速sigmoid系数
  decel_sigmoid_k: 4.0              # 减速sigmoid系数
  accel_sigmoid_center: 0.9          # 加速sigmoid中心点
  decel_sigmoid_center: 0.5          # 减速sigmoid中心点


# 距离阈值
distance:
  alignment: 0.08                    # 对齐距离阈值 [m]
  deceleration: 0.25                  # 减速距离 [m]
  acceleration: 0.15                 # 加速阶段距离 [m]
  lookahead: 0.15                    # 前瞻基础距离 [m]
  lookahead_time: 0.05                # 前瞻时间系数 [s], 总前瞻 = lookahead + lookahead_time * speed
  waypoint_tolerance: 0.01          # 路径点容差 [m]


# 滤波器配置
filter:
  # 位置滤波
  pos_hampel_window: 5               # Hampel窗口大小
  pos_hampel_k: 3.0                  # Hampel异常值阈值
  pos_savgol_window: 7               # Savitzky-Golay窗口大小
  pos_savgol_order: 3                # Savitzky-Golay多项式阶数
  # 角速度滤波
  angular_hampel_window: 5           # Hampel窗口大小
  angular_hampel_k: 3.0              # Hampel异常值阈值


# 起步回线 / 进线制导（决定 handlePathFollowing 里目标航向生成方式）
guidance:
  mode: "weighted_yaw"                     # "weighted_yaw" | "stanley" | "hybrid"
  alignment_mode: "stanley"          # mode=hybrid 时生效：对齐阶段制导
  following_mode: "weighted_yaw"     # mode=hybrid 时生效：跟随阶段制导
  stanley_k: 1.0                     # stanley: 增益 k
  stanley_v0: 0.1                   # stanley: 低速软化项 v0 [m/s]
  theta_max: 0.25                     # 最大截获角限制 [rad]


# 阶段控制参数
phase:
  alignment:                         # 对齐阶段
    max_angular_vel: 0.08           # 最大角速度 [rad/s]
    max_angular_accel: 0.07          # 最大角加速度 [rad/s²]
    current_heading_weight: 0.0      # 当前航向权重
    target_heading_weight: 1.0       # 目标航向权重
    filter_alpha: 0.8               # 低通滤波系数

    smoother_freq: 15.0              # 平滑器频率 [Hz]
    smoother_damping: 0.86           # 平滑器阻尼比
    line_cross_track_tolerance: 0.003 # 起步：到直线的横向误差阈值 [m]
    line_heading_tolerance: 0.02     # 起步：相对直线航向的误差阈值 [rad]
    line_alignment_stable_count: 10   # 起步：连续满足次数（抗抖）

  following:                         # 跟随阶段
    max_angular_vel: 0.065           # 最大角速度 [rad/s]
    max_angular_accel: 0.05          # 最大角加速度 [rad/s²]
    current_heading_weight: 0.0      # 当前航向权重
    target_heading_weight: 1.0       # 目标航向权重
    filter_alpha: 0.95               # 低通滤波系数
    
    smoother_freq: 15.0              # 平滑器频率 [Hz]
    smoother_damping: 0.86           # 平滑器阻尼比


# 原地旋转控制
rotation:
  max_w: 0.8                         # 最大角速度 [rad/s]
  min_w: 0.15                        # 最小角速度 [rad/s]
  factor: 1.2                        # 角速度调节因子
  angle_threshold: 0.5               # 角度阈值 [rad]
  smooth_factor: 0.6                 # 平滑因子
