# Request
string printer_name      # 打印机名称: "left", "center", "right"
string content          # 要打印的内容（文字）
int32 font_size         # 字体大小（可选，默认12）
bool repeat             # 是否重复打印（可选，默认false）

---

# Response
bool success
string message
