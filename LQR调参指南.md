# LQR路径跟踪控制器调参详细指南

## 目录
- [1. LQR控制原理](#1-lqr控制原理)
- [2. 控制架构](#2-控制架构)
- [3. 核心参数详解](#3-核心参数详解)
- [4. 调参步骤与策略](#4-调参步骤与策略)
- [5. 常见问题与解决方案](#5-常见问题与解决方案)
- [6. 高级调参技巧](#6-高级调参技巧)

---

## 1. LQR控制原理

### 1.1 什么是LQR？

LQR（Linear Quadratic Regulator，线性二次型调节器）是一种最优控制方法，通过最小化代价函数来计算最优反馈增益。

**代价函数**：
```
J = ∫[x^T·Q·x + u^T·R·u]dt
```

其中：
- `x` = 状态误差向量 = `[e_y, e_θ]^T`
  - `e_y`：横向位置误差（机器人到路径的垂直距离）
  - `e_θ`：航向角误差（机器人朝向与路径切线方向的夹角）
- `u` = 控制输入 = 角速度增量
- `Q` = 状态权重矩阵（对角阵）= `diag[q1, q2]`
- `R` = 控制权重

### 1.2 运动学模型

假设机器人为差分驱动模型，在Frenet坐标系下的误差动态方程为：

```
ė_y = v·sin(e_θ)  ≈ v·e_θ     (小角度近似)
ė_θ = ω - v·κ
```

其中：
- `v`：线速度
- `ω`：角速度（控制量）
- `κ`：路径曲率

### 1.3 LQR反馈控制律

通过求解Riccati方程，得到最优反馈增益 `K = [K1, K2]`，控制律为：

```
ω_fb = -K1·e_y - K2·e_θ
```

**增益计算公式**：
```
K1 = √(q1/r) / v
K2 = √(2√(q1·q2)/r + q2/r)
```

**关键特性**：
- `K1` 与速度 `v` 成**反比**：低速时需要更大的增益来纠正横向误差
- `K2` 与速度**无关**：航向误差的纠正不依赖速度
- `q1/q2` 的比值决定了横向误差和航向误差的相对重要性

---

## 2. 控制架构

### 2.1 完整控制流程

```
┌─────────────────────────────────────────────────────────────┐
│                      LQR路径跟踪控制器                        │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  1. 最近点搜索（双向搜索窗口 + 弧长）              │
    │     输出：nearest_idx, e_y, e_θ                   │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  2. 前瞻点计算（基于弧长 + 时间前瞻）              │
    │     lookahead_s = distance + time × v             │
    │     输出：ref_point (x, y, θ, κ)                  │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  3. 增益计算（基于当前速度 v）                     │
    │     use_direct_gains? K1/K2_direct : 从Q/R计算    │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  4. 前馈控制                                       │
    │     ω_ff = v × κ                                  │
    │     （补偿路径曲率，保证圆形路径稳定跟踪）          │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  5. LQR反馈控制                                    │
    │     ω_fb = -K1·e_y - K2·e_θ                       │
    │     （纠正位置和航向误差）                         │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  6. 积分项（可选，用于消除静态误差）                │
    │     integral_e_y = decay × integral + e_y × dt    │
    │     ω_i = -Ki × integral_e_y                      │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  7. 总控制量                                       │
    │     ω = ω_ff + ω_fb + ω_i                         │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
    ┌──────────────────────────────────────────────────┐
    │  8. 限幅与平滑（角速度/角加速度限制）               │
    │     |ω| ≤ omega_max                               │
    │     |ω̇| ≤ omega_dot_max                          │
    └──────────────────────────────────────────────────┘
                              │
                              ▼
                        cmd_vel (v, ω)
```

### 2.2 控制量组成

```
总角速度 = 前馈 + 反馈 + 积分
ω = ω_ff + ω_fb + ω_i
  = v·κ + (-K1·e_y - K2·e_θ) + (-Ki·∫e_y·dt)
```

**各部分作用**：
- **前馈** `ω_ff = v·κ`：根据路径曲率提供基础角速度，保证圆弧路径跟踪
- **反馈** `ω_fb`：根据当前误差进行纠正
- **积分** `ω_i`（可选）：消除持续性静态误差

---

## 3. 核心参数详解

### 3.1 LQR增益参数

#### 方式1：直接指定增益（推荐）

```yaml
lqr:
  use_direct_gains: true
  K1_direct: 6.0    # 横向误差增益
  K2_direct: 6.0    # 航向误差增益
```

**优点**：
- 直观易调，无需理解Q/R矩阵
- 增益固定，不随速度变化
- 适合速度范围较窄的应用

**调参建议**：
- **K1（横向误差增益）**：
  - 初始值：`5.0 ~ 10.0`
  - 增大 → 更快纠正横向偏差，但可能引起振荡
  - 减小 → 响应温和，但收敛慢
  - 圆形路径建议用**较小值**（6.0），避免过度纠正
  - 直线路径可用**较大值**（20.0），快速回到路径

- **K2（航向误差增益）**：
  - 初始值：`5.0 ~ 10.0`
  - 增大 → 航向对齐更快，但可能导致角速度震荡
  - 减小 → 平滑但航向纠正慢
  - 一般设置为 `K2 ≈ K1`

**速度影响**：
- 直接增益模式下，K1和K2与速度无关
- 但实际控制效果仍受速度影响：`ω_fb = -K1·e_y - K2·e_θ`
- 建议针对主要运行速度进行调参

#### 方式2：从Q/R矩阵计算增益

```yaml
lqr:
  use_direct_gains: false
  q1: 1.0      # 横向误差权重
  q2: 200.0    # 航向误差权重
  r: 1.0       # 控制量权重
```

**计算公式**：
```
K1 = √(q1/r) / v
K2 = √(2√(q1·q2)/r + q2/r)
```

**调参建议**：
- **q1（横向误差权重）**：
  - 增大 → K1增大 → 更重视横向误差
  - 典型值：`0.5 ~ 5.0`

- **q2（航向误差权重）**：
  - 增大 → K2增大 → 更重视航向误差
  - 典型值：`50.0 ~ 500.0`
  - 通常 `q2 >> q1`，因为航向误差会累积导致横向误差增大

- **r（控制量权重）**：
  - 增大 → K1/K2减小 → 控制更温和，节能
  - 减小 → K1/K2增大 → 控制更激进，响应快
  - 典型值：`0.5 ~ 2.0`

**关键比值**：
- `q1/r` 决定 K1 的大小
- `q2/r` 决定 K2 的大小
- `q2/q1` 决定航向误差相对横向误差的重要性（通常设置为 100~500）

---

### 3.2 积分参数（LQI）

```yaml
lqr:
  enable_integral: false    # 是否启用积分项
  Ki: 0.5                   # 积分增益
  integral_max: 0.1         # 积分饱和限制 (rad/s)
  integral_decay: 0.99      # 积分衰减系数
```

**作用**：
- 消除静态误差（如轮子半径不一致、地面倾斜导致的持续偏差）
- 积分项 `ω_i = -Ki × ∫e_y·dt`

**调参建议**：
- **初期建议关闭**（`enable_integral: false`），先调好PD控制
- 如果发现持续性偏差（误差不为零但稳定），再开启积分

- **Ki（积分增益）**：
  - 初始值：`0.1 ~ 0.5`
  - 过大会导致积分饱和和振荡
  - 过小则消除静态误差的速度慢

- **integral_max**：
  - 积分项的饱和限制，防止积分过大
  - 建议设置为 `omega_max` 的 20%~50%

- **integral_decay**：
  - 积分衰减系数，防止历史误差累积
  - `0.95 ~ 0.99`，越小衰减越快

---

### 3.3 速度参数

```yaml
velocity:
  max: 0.05              # 最大线速度 (m/s)
  min: 0.01              # 最小线速度 (m/s)
  omega_max: 0.3         # 最大角速度 (rad/s)
  omega_dot_max: 0.5     # 最大角加速度 (rad/s²)
```

**调参建议**：

- **max（最大线速度）**：
  - 根据控制频率和期望精度确定
  - 高精度应用：`0.03 ~ 0.08 m/s`
  - 一般应用：`0.1 ~ 0.3 m/s`
  - 经验公式：`v_max ≈ 控制频率 × 期望精度`
    - 例：18Hz × 5mm = 0.09 m/s

- **omega_max（最大角速度）**：
  - 必须满足路径曲率需求：`omega_max ≥ v_max × κ_max`
  - 圆形路径示例：
    - 半径 = 0.5m → κ = 2.0
    - v = 0.05 m/s → ω_ff = 0.1 rad/s
    - 留20%余量用于反馈 → `omega_max = 0.3 rad/s`

- **omega_dot_max（最大角加速度）**：
  - 限制角速度变化率，平滑控制
  - **降低此值可以有效减少振荡**
  - 典型值：`0.3 ~ 1.0 rad/s²`
  - 过小会导致响应迟缓

---

### 3.4 前瞻参数

```yaml
lookahead:
  distance: 0.035    # 基础前瞻距离 (m)
  time: 0.3          # 时间前瞻系数 (s)
```

**前瞻距离计算**：
```
lookahead_s = distance + time × v
```

**调参建议**：

- **distance（基础前瞻距离）**：
  - 最小前瞻距离，避免速度为零时前瞻点就在脚下
  - 经验值：`2 ~ 5` 倍控制周期距离
    - 例：18Hz，v=0.05m/s → 控制周期距离 = 0.05/18 ≈ 0.003m
    - 前瞻距离：`0.003 × 10 = 0.03m`
  - 典型值：`0.02 ~ 0.1 m`

- **time（时间前瞻）**：
  - 与速度相关的动态前瞻
  - 增大 → 前瞻更远 → 更提前预测，适合高曲率路径
  - 减小 → 前瞻更近 → 更跟随当前位置
  - 典型值：`0.2 ~ 0.5 s`

**不同路径类型建议**：
- **直线路径**：前瞻可以较小，`distance=0.02, time=0.2`
- **圆形路径**：需要更大前瞻，`distance=0.035, time=0.3`
- **复杂曲线**：适中前瞻，`distance=0.03, time=0.25`

---

### 3.5 搜索窗口参数

```yaml
search:
  window_back: 10      # 向后搜索窗口
  window_forward: 50   # 向前搜索窗口
```

**作用**：
- 在路径点中搜索最近点，避免索引震荡和错误匹配

**调参建议**：
- **window_back**：
  - 防止在弯道处索引回退
  - 一般设置较小：`5 ~ 20`

- **window_forward**：
  - 高曲率路径需要更大窗口
  - 圆形路径：`30 ~ 100`
  - 直线路径：`10 ~ 30`

---

## 4. 调参步骤与策略

### 4.1 调参流程图

```
开始
  │
  ▼
[1] 设置基础参数
  │  - 速度、前瞻、搜索窗口
  │  - 使用保守值
  │
  ▼
[2] 调整LQR增益（仅反馈）
  │  - 关闭积分项
  │  - 从小增益开始
  │  - 逐步增大K1和K2
  │
  ▼
[3] 测试不同路径
  │  - 直线、圆形、复杂曲线
  │  - 观察误差和振荡
  │
  ▼
[4] 优化速度和前瞻
  │  - 根据精度要求调整v_max
  │  - 调整lookahead适应路径
  │
  ▼
[5] 微调限幅参数
  │  - omega_max, omega_dot_max
  │  - 平滑控制输出
  │
  ▼
[6] 启用积分项（可选）
  │  - 仅当有持续偏差时启用
  │  - 小心调整Ki
  │
  ▼
结束
```

---

### 4.2 分步调参指南

#### 步骤1：设置基础参数（保守策略）

```yaml
# 使用较低速度开始测试
velocity:
  max: 0.03
  omega_max: 0.3
  omega_dot_max: 0.5

# 中等前瞻距离
lookahead:
  distance: 0.03
  time: 0.3

# 较大搜索窗口
search:
  window_back: 10
  window_forward: 50

# 关闭积分
lqr:
  enable_integral: false
```

#### 步骤2：调整LQR增益

**推荐使用直接增益模式**：

```yaml
lqr:
  use_direct_gains: true
  K1_direct: 5.0    # 从小值开始
  K2_direct: 5.0
```

**调参过程**：

1. **初始测试**（K1=5, K2=5）：
   - 运行路径跟踪，观察：
     - 横向误差 `e_y` 是否快速收敛
     - 航向误差 `e_θ` 是否稳定
     - 角速度 `ω` 是否平滑

2. **调整K1（横向误差增益）**：
   - 如果横向误差收敛慢 → 增大K1（每次增加20%~50%）
   - 如果出现左右摆动 → 减小K1
   - 目标：快速收敛且无振荡

3. **调整K2（航向误差增益）**：
   - 如果航向对齐慢 → 增大K2
   - 如果角速度震荡 → 减小K2
   - 目标：平滑对齐路径方向

4. **协调K1和K2**：
   - 一般设置 `K2 ≈ K1` 或略大
   - 圆形路径：降低K1（减少横向纠正，依赖前馈）
   - 直线路径：可以增大K1（快速纠偏）

**典型增益值**：
```yaml
# 圆形路径（v=0.05m/s）
K1_direct: 6.0
K2_direct: 6.0

# 直线路径（v=0.05m/s）
K1_direct: 15.0
K2_direct: 10.0

# 复杂曲线（v=0.05m/s）
K1_direct: 10.0
K2_direct: 8.0
```

#### 步骤3：优化速度和前瞻

**速度调整**：
- 逐步提高 `v_max`（每次增加20%）
- 观察误差是否增大
- 如果使用Q/R矩阵模式，增益会自动适配速度

**前瞻调整**：
- 如果在弯道处误差大 → 增大前瞻（`time +0.1`）
- 如果响应迟缓 → 减小前瞻
- 经验：`lookahead_s ≈ v × (0.3~0.5)`

#### 步骤4：调整限幅参数

**角速度限幅**：
```yaml
omega_max: v_max × κ_max × 1.5  # 留50%余量
```

**角加速度限幅（重要）**：
- **降低 `omega_dot_max` 可显著减少振荡**
- 从 `0.5` 开始，如有震荡降至 `0.3`
- 代价是响应变慢

#### 步骤5：启用积分项（可选）

**仅在以下情况启用**：
- 误差稳定后仍有固定偏差（如始终偏左或偏右）
- PD控制已调优但无法消除静态误差

**调参步骤**：
```yaml
lqr:
  enable_integral: true
  Ki: 0.2              # 从小值开始
  integral_max: 0.05   # omega_max的20%
  integral_decay: 0.98
```

- 逐步增大 `Ki`，观察偏差是否消除
- 如果出现振荡，降低 `Ki` 或增大 `integral_decay`

---

### 4.3 不同场景的调参策略

#### 场景1：高精度低速跟踪（如AGV仓库作业）

```yaml
velocity:
  max: 0.05
  omega_max: 0.3
  omega_dot_max: 0.3    # 平滑优先

lqr:
  use_direct_gains: true
  K1_direct: 8.0        # 较高增益，快速纠偏
  K2_direct: 8.0
  enable_integral: true # 消除静态误差
  Ki: 0.3

lookahead:
  distance: 0.035
  time: 0.3
```

#### 场景2：中速一般精度（如清扫机器人）

```yaml
velocity:
  max: 0.15
  omega_max: 0.8
  omega_dot_max: 1.0

lqr:
  use_direct_gains: false  # 使用Q/R自适应速度
  q1: 1.0
  q2: 150.0
  r: 1.0
  enable_integral: false

lookahead:
  distance: 0.05
  time: 0.25
```

#### 场景3：圆形路径跟踪

```yaml
velocity:
  max: 0.05
  omega_max: 0.3        # v×κ = 0.05×2.0 = 0.1，留余量

lqr:
  K1_direct: 6.0        # 较小增益，依赖前馈
  K2_direct: 6.0

lookahead:
  distance: 0.035       # 较大前瞻
  time: 0.3

search:
  window_forward: 50    # 大搜索窗口
```

---

## 5. 常见问题与解决方案

### 问题1：机器人左右摆动（横向振荡）

**现象**：
- `e_y` 在路径两侧来回振荡
- 角速度 `ω` 频繁变号

**原因分析**：
- K1（横向增益）过大
- 角加速度限制太宽松
- 前瞻距离过小

**解决方案**：
```yaml
# 方案1：降低K1
lqr:
  K1_direct: 6.0  # 从10.0降至6.0

# 方案2：限制角加速度
velocity:
  omega_dot_max: 0.3  # 从0.5降至0.3

# 方案3：增大前瞻
lookahead:
  distance: 0.04  # 从0.03增至0.04
  time: 0.35      # 从0.3增至0.35
```

---

### 问题2：航向角震荡

**现象**：
- `e_θ` 振荡
- 机器人频繁左右扭动

**原因分析**：
- K2（航向增益）过大

**解决方案**：
```yaml
lqr:
  K2_direct: 5.0  # 降低航向增益
```

---

### 问题3：无法跟上路径曲率（圆弧切内侧）

**现象**：
- 在圆形路径上，机器人始终偏向圆心
- 横向误差持续为负

**原因分析**：
- 前馈不足（速度过高或角速度限制太小）
- 前瞻距离太小

**解决方案**：
```yaml
# 方案1：增大角速度限制
velocity:
  omega_max: 0.4  # 增大到 v×κ×1.5以上

# 方案2：降低速度
velocity:
  max: 0.04       # 从0.05降至0.04

# 方案3：增大前瞻
lookahead:
  distance: 0.04
  time: 0.4
```

---

### 问题4：响应迟缓，误差收敛慢

**现象**：
- 起步后需要很长时间才接近路径
- 误差缓慢下降

**原因分析**：
- K1和K2过小
- 速度过低
- 角加速度限制太严

**解决方案**：
```yaml
# 方案1：增大增益
lqr:
  K1_direct: 12.0  # 提高增益
  K2_direct: 10.0

# 方案2：放宽角加速度限制
velocity:
  omega_dot_max: 0.8
```

---

### 问题5：存在持续性偏差（静态误差）

**现象**：
- `e_y` 稳定在某个非零值（如始终偏左5mm）
- 不是振荡，而是稳态偏差

**原因分析**：
- 模型误差（轮子半径不一致）
- 地面倾斜
- 缺少积分项

**解决方案**：
```yaml
# 启用积分项
lqr:
  enable_integral: true
  Ki: 0.3
  integral_max: 0.1
  integral_decay: 0.98
```

---

### 问题6：最近点索引跳跃

**现象**：
- `debug_ref_index` 频繁大幅变化
- 路径跟踪不稳定

**原因分析**：
- 搜索窗口太小
- 路径点间距不均匀

**解决方案**：
```yaml
search:
  window_back: 15      # 增大搜索窗口
  window_forward: 80
```

---

## 6. 高级调参技巧

### 6.1 速度自适应增益

使用Q/R矩阵模式可实现速度自适应：

```yaml
lqr:
  use_direct_gains: false
  q1: 1.0
  q2: 200.0
  r: 1.0
```

**优势**：
- K1会随速度自动调整：`K1 = √(q1/r) / v`
- 低速时K1大（强纠偏），高速时K1小（避免振荡）

**适用场景**：
- 速度变化范围大（如0.05~0.3 m/s）
- 需要在不同速度下保持一致的控制性能

---

### 6.2 路径类型自适应

**建议**：针对不同路径类型准备多套参数

**示例**：
```yaml
# config/lqr_circular.yaml  - 圆形路径专用
lqr:
  K1_direct: 6.0
  K2_direct: 6.0
lookahead:
  distance: 0.035
  time: 0.3

# config/lqr_straight.yaml  - 直线路径专用
lqr:
  K1_direct: 15.0
  K2_direct: 10.0
lookahead:
  distance: 0.025
  time: 0.2
```

在代码中根据路径曲率动态切换配置。

---

### 6.3 调参日志分析

**启用详细调试输出**：
```yaml
debug:
  enable: true
  verbose: true
```

**关键输出**：
```
LQR控制: 横向误差=5.2mm, 航向角误差=1.2°,
         前馈角速度=0.100, 反馈角速度=-0.025,
         总角速度=0.075, 曲率=2.000
```

**分析要点**：
- `横向误差` > 10mm → 增大K1或降低速度
- `航向角误差` > 5° → 增大K2
- `反馈角速度` 频繁变号 → K1或K2过大
- `总角速度` 接近 `omega_max` → 增大限制或降速

---

### 6.4 栅格图可视化

**启用栅格图可视化**：
```yaml
debug:
  enable_grid_map: true
  grid_map_path: 'grid_maps'
```

**作用**：
- 可视化路径、机器人位置、误差
- 帮助诊断最近点匹配问题

---

### 6.5 参数敏感性分析

**各参数对性能的影响程度**：

| 参数 | 影响 | 敏感度 | 调整优先级 |
|------|------|--------|-----------|
| K1_direct | 横向误差收敛速度 | 高 | ★★★★★ |
| K2_direct | 航向对齐速度 | 高 | ★★★★★ |
| omega_dot_max | 平滑性 | 高 | ★★★★☆ |
| lookahead.time | 弯道预测 | 中 | ★★★☆☆ |
| v_max | 速度与精度平衡 | 中 | ★★★☆☆ |
| Ki | 静态误差消除 | 中 | ★★☆☆☆ |
| window_forward | 搜索稳定性 | 低 | ★★☆☆☆ |

**建议调参顺序**：
1. K1/K2（增益）
2. omega_dot_max（平滑性）
3. v_max（速度）
4. lookahead（前瞻）
5. Ki（积分，可选）

---

## 7. 调参检查清单

### 启动前检查

- [ ] 是否设置了合理的 `v_max` 和 `omega_max`？
- [ ] `omega_max` 是否满足 `≥ v_max × κ_max × 1.5`？
- [ ] 是否关闭了积分项（首次调参）？
- [ ] 是否启用了调试日志？

### 初步调参检查

- [ ] K1和K2是否从保守值开始（如5.0）？
- [ ] 是否在简单路径（直线或圆形）上测试？
- [ ] 是否观察到了误差收敛趋势？
- [ ] 是否有明显振荡？

### 优化阶段检查

- [ ] 横向误差是否收敛到期望范围（如±5mm）？
- [ ] 航向误差是否足够小（±2°）？
- [ ] 角速度是否平滑（无频繁抖动）？
- [ ] 不同速度下是否都表现良好？
- [ ] 是否测试了不同曲率的路径？

### 最终验证检查

- [ ] 是否在实际任务路径上测试？
- [ ] 长时间运行是否稳定（无累积误差）？
- [ ] 是否记录了最终参数配置？
- [ ] 是否备份了栅格图和日志用于后续分析？

---

## 8. 参考资料与进阶学习

### 理论基础
- LQR最优控制理论
- Frenet坐标系与路径跟踪
- Riccati方程求解

### 推荐文献
- Snider, J. M. (2009). "Automatic Steering Methods for Autonomous Automobile Path Tracking"
- Paden, B., et al. (2016). "A Survey of Motion Planning and Control Techniques for Self-Driving Urban Vehicles"

### 调参工具建议
- 实时误差曲线绘制（rqt_plot）
- 参数动态调整（dynamic_reconfigure）
- 栅格图可视化分析

---

## 附录：快速参考表

### 常用参数组合

| 应用场景 | v_max | K1 | K2 | lookahead | omega_dot_max |
|---------|-------|----|----|-----------|---------------|
| 高精度AGV | 0.05 | 8.0 | 8.0 | 0.035m, 0.3s | 0.3 |
| 圆形路径 | 0.05 | 6.0 | 6.0 | 0.035m, 0.3s | 0.5 |
| 直线路径 | 0.05 | 15.0 | 10.0 | 0.025m, 0.2s | 0.8 |
| 中速清扫 | 0.15 | - | - | 0.05m, 0.25s | 1.0 |

注：中速清扫建议使用Q/R自适应模式（q1=1.0, q2=150.0, r=1.0）

---

**版本**: v1.0
**更新日期**: 2026-01-06
**适用控制器**: xline_follow_controller (LQR)
